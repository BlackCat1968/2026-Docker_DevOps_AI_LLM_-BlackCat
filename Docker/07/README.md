# 映像檔最佳化：多階段建置、BuildKit 快取與瘦身

映像檔瘦身就是「把出貨的箱子跟工作的廠房分開」：建置需要的工具留在廠房，箱子裡只裝跑起來真正需要的東西。

第 06 章你已經會寫正確的 Dockerfile，這一章把它練成精實的 Dockerfile。三把大刀依序出鞘：多階段建置砍掉建置工具、BuildKit 快取掛載讓重建飛快、buildx 一次吐出多架構映像檔——每一刀都有前後對照的實測數字。